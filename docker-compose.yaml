services:
  solace-db:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: solace
      POSTGRES_PASSWORD: strong-password
      POSTGRES_DB: solace
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
